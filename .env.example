# Copy to .env.ssh (gitignored). Do not commit .env.ssh.
TABASAMU_SSH_HOST=169.58.241.255
TABASAMU_SSH_USER=root
# TABASAMU_SSH_PORT=22
# TABASAMU_SSH_KEY=~/.ssh/id_rsa
# Optional — avoids typing password each time (file stays local only):
# TABASAMU_SSH_PASSWORD=your-password-here
